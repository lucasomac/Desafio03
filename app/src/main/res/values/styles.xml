<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Desafio03.ActionBar.SubTitleTextStyle" parent="Theme.MaterialComponents.Light">
        <!-- Altere a cor abaixo -->
        <item name="android:textColor">@color/white</item>
        <!-- Altere a cor abaixo, acredito que essa regra nao eh necessaria -->
        <item name="android:actionMenuTextColor">@color/white</item>
        <!-- Altere a cor abaixo, acredito que essa regra nao eh necessaria -->
        <item name="android:subtitleTextStyle">@color/white</item>
    </style>

    <style name="ProgressBarStyle">
        <item name="colorAccent">@color/primary</item>
    </style>

    <style name="Theme.DesafioAndroid03.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="boxBackgroundColor">@android:color/transparent</item>
        <item name="android:textColor">@color/primary</item>
        <item name="android:textColorHint">@color/primary</item>
        <item name="boxStrokeColor">@color/gray</item>
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/gray</item>
    </style>
</resources>